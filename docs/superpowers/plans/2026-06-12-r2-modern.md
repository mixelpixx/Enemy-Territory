# R2-5: Modern Renderer Features ON Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Every modern renderer2 feature — HDR/tonemap/bloom, shadow mapping (incl. soft shadows and sun cascades), per-pixel dynamic lights, normal/specular/parallax materials, water/reflection, plus the bonus post-effects (SSAO, depth of field, camera post-FX) — works when switched ON under `cl_renderer gl2`, each behind its own cvar, all default-OFF, with the all-off state verified pixel-equivalent to the accepted R2-4 baseline.

**Architecture:** Research verified (2026-06-12): all features are compiled into the DLL already; every modern cvar defaults OFF upstream and nothing in our scripts/bridge/cmake overrides them — so the accepted R2-4 baseline IS the all-off state, and this increment is bring-up of the ON states. Per-feature kill-switches are the descope mechanism: a feature that proves broken is documented as known-off and the increment still ships. One structural risk to clear first: the bridge cvar proxy narrows their 64-bit cvar flags to our int via `BRDG_CVAR_FLAGMASK` (bits 0-13, tr2_bridge_theirs.c) — their `CVAR_ARCHIVE_ND` and `CVAR_LATCH` bit values must be confirmed to survive the narrowing, or latched/archived behavior of every modern cvar silently breaks (Task 1).

**Feature cvar map (from the research pass; upstream file refs are identical in our vendored tree `src/renderer2/etl/`):**
| Feature | Cvar(s) | Default | Latch? | GL caps needed |
|---|---|---|---|---|
| HDR + tonemap | r_hdrRendering (+ r_hdrKey/MinLuminance/MaxLuminance/ToneMappingOperator/Gamma — CHEAT) | 0 | LATCH | FBO + textureFloat + framebufferBlit (HDR_ENABLED macro, their tr_local.h:94) |
| Bloom | r_bloom, r_bloomBlur (r_bloomPasses CHEAT) | 0 | live | FBO |
| Shadow mapping | r_shadows (0..5 modes), r_softShadows (0..6: ESM/PCF), r_shadowMapQuality (0-4), r_shadowMapSize*, r_parallelShadowSplits (sun cascades) | shadows=1 (blur mode, NOT mapping) | LATCH | FBO + depth/float textures |
| Dynamic lights | r_dynamicLight (already 1 = default ON in baseline), r_dynamicLightShadows (1) | on | live | none |
| Normal/spec/parallax | r_normalMapping, r_parallaxMapping, r_specularScale/Exponent | 0 | LATCH | shader-only |
| Reflection/water | r_reflectionMapping | 0 | LATCH | cubemaps; UPSTREAM STATUS UNCLEAR (registered but consumer not located — investigate, may be dead) |
| SSAO | r_screenSpaceAmbientOcclusion (0-2) | 0 | live | FBO |
| Depth of field | r_depthOfField | 0 | live | FBO |
| Camera FX | r_cameraPostFX, r_cameraVignette, r_cameraFilmGrainScale | 0/1/3 (postFX master = 0) | live | none |
| Rotoscope | r_rotoscope, r_rotoscopeBlur | 0 | live | none |

Retail content note: normal/specular/parallax silently no-op on stock textures (no _nm/_s maps in the 2003 paks, no generated fallbacks) — enabling them must produce ZERO visual change on stock maps; proving they actually work needs a small test material (Task 4).

**Tech Stack:** as R2-2..R2-4 (MSVC x64 via vcvars64, CMake/Ninja, the bridge). Reference clone: `C:\repo\et-deps\etlegacy` v2.84.0. User GPU: AMD Radeon 780M, GL 4.6 (all caps present).

**Edit rules (unchanged):** vendored `etl*` untouchable (mechanical MSVC-only edits, logged in docs/THIRDPARTY.md); engine off-limits except provably gl2-gated; bridge freely editable (links only into the DLL); gl1 byte-identical; ctest 3/3 (pmove_feel golden c1a6e7471a5e279d) after every task. CHEAT-flagged tuning cvars stay cheat-protected — we expose the ARCHIVE_ND master switches, not the internals.

**Build (PowerShell tool, NOT Bash):**
```
cmd /c '"C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat" >nul 2>&1 && cmake -S C:\repo\et-rm -B C:\repo\et-rm\build -G Ninja -DCMAKE_BUILD_TYPE=Debug && cmake --build C:\repo\et-rm\build && ctest --test-dir C:\repo\et-rm\build --output-on-failure'
```
**Launch (exe `etrm.exe`; gl2 + known-good harness flags):**
```
& C:\repo\et-rm\build\bin\etrm.exe +set fs_basepath "C:\repo\enemy-territory-RM" +set fs_homepath "C:\repo\et-rm\build\bin" +set sv_pure 0 +set r_fullscreen 0 +set developer 1 +set logfile 2 +set cl_renderer gl2 +set r_screenshotFormat 0 +set com_hunkmegs 256 +set g_gametype 2 <scenario>
```
Harness gotchas (established R2-4): `team <b|r> [class] [weapon]` spawns; `setviewpos x y z yaw` = deterministic viewpoints (devmap cheats — USE THESE for every A/B shot); cfg `wait` counts frames; `+demo X` precedes the exec'd harness; latched cvars need them set on the COMMAND LINE (`+set r_hdrRendering 1`) or a vid_restart after setting; r_gamma stays 1.0 for all screenshot comparisons (gl2 screenshots double-bake gamma otherwise). Log: `build\bin\etmain\etconsole.log`; archive per-task as `etconsole_r5_<task>_<scenario>.log`. Check `build\bin` for *.dmp after every run. NEVER write into C:\repo\enemy-territory-RM. python+numpy available for TGA pixel metrics.

**Evidence conventions:** screenshots `r5t<N>_<feature>_<state>.tga` in build\bin\etmain\screenshots\. Every feature gets: (a) an OFF shot and an ON shot at the SAME setviewpos, (b) a pixel-diff metric proving the feature visibly changed the frame (or, for the no-op-on-retail materials, proving it did NOT), (c) log grep clean of new per-frame warnings/GL errors (renderer2 logs GL errors via GL_CheckErrors — grep "GL_" and "ERROR").

**Git:** branch `rm/10-r2-modern` off `main` (already created). Merge `--no-ff` to `main` after USER acceptance, tag `rm-r2-5`. Commit trailer (exact last line):
```
Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

---

## Task 1: Baseline lock + cvar-proxy flag audit

The increment's correctness anchor. Two jobs: (1) capture the all-off baseline screenshots every later task diffs against; (2) confirm the bridge cvar proxy preserves the two flag semantics every modern cvar depends on — LATCH (takes effect at vid_restart, not silently never) and ARCHIVE_ND (persists to etconfig.cfg) — across the 64-bit→int narrowing.

**Files:**
- Possibly modify: `src/renderer2/bridge/tr2_bridge_theirs.c` (BRDG_CVAR_FLAGMASK / flag translation) — only if the audit finds dropped bits
- Create: nothing else (screenshots/logs are build-dir artifacts)

- [x] **Step 1:** Read their flag definitions (`src/renderer2/etlhdr/qcommon/q_shared.h` — cvarFlags_t: CVAR_ARCHIVE, CVAR_LATCH, CVAR_ARCHIVE_ND, CVAR_NODEFAULT bit values) and ours (`src/game/q_shared.h`). Table them. Determine: do CVAR_LATCH and the ARCHIVE bit inside ARCHIVE_ND map to the same bit values in our world and survive `BRDG_CVAR_FLAGMASK 0x3FFF`? Is CVAR_NODEFAULT a high bit that gets dropped (acceptable — it only suppresses config writing of default values) or does dropping it change behavior? — *AUDIT PASSED, NO FIX NEEDED. Flag table (theirs etlhdr/qcommon/q_shared.h:1036-1064 vs ours src/game/q_shared.h:952-973):*
  | Flag | Their bit | Our bit | Survives 0x3FFF? | Verdict |
  |---|---|---|---|---|
  | CVAR_ARCHIVE | BIT(0)=1 | 1 | yes | identical value+meaning |
  | CVAR_LATCH | BIT(5)=32 | 32 | yes | identical value+meaning |
  | CVAR_NODEFAULT | BIT(19) | (none) | NO — dropped | acceptable: ARCHIVE_ND degrades to plain ARCHIVE; default-valued moderns get written to config as `seta r_x "0"` instead of omitted — cosmetic only |
  | CVAR_ARCHIVE_ND | BIT(0)\|BIT(19) | n/a | ARCHIVE half survives | archive semantics preserved |

  *All bits 0-13 (ARCHIVE..SERVERINFO_NOUPDATE) are value-identical between the two headers; their bit14+ (SERVER_CREATED, VM_CREATED, PROTECTED, SHADER, NOTABCOMPLETE, NODEFAULT, MODIFIED, NONEXISTENT) have no our-side equivalent and are correctly logged-once-and-dropped by the mask. Renderer-side checks of high bits (e.g. CVAR_SHADER) still work because the proxy stores the FULL unmasked 64-bit flags in `p->proxy.flags` (tr2_bridge_theirs.c imp_Cvar_Get) — only the our-engine copy is narrowed.*
- [x] **Step 2:** Empirical check: gl2 launch, `+set r_bloom 1` at the console (live cvar) and `r_hdrRendering 1` at the console (latched — expect the engine's "will be changed upon restarting" message), then `vid_restart`, then `cvarlist r_hdr*` — confirm the latch released. Quit; confirm `r_bloom "1"` was archived into build\bin\etmain\etconfig.cfg (then reset it to 0 and confirm it disappears or writes 0 — ND semantics). If any of this fails, fix the proxy flag translation (bridge-only edit, follow the existing BRDG_CVAR_FLAGMASK comment block) and re-verify. — *ALL SEMANTICS PASS, zero code changes. LATCH: `set r_hdrRendering 1` → `r_hdrRendering will be changed upon restarting.`; after `vid_restart` the latch released (`Cvar_Set2: r_hdrRendering 1` during renderer reinit; `cvarlist` shows `AL  r_hdrRendering "1"` — Archive+Latch flags live on the engine cvar). ARCHIVE: quit wrote `seta r_bloom "1"` / `seta r_hdrRendering "1"`. Full round-trip (write AND read-back) proven with `+set cl_profile rmtest`: second launch's `cvarlist` showed `A r_bloom "1"` / `AL r_hdrRendering "1"` restored from profiles/rmtest/etconfig.cfg, and setting them back to 0 re-latched (`will be changed upon restarting.`) and persisted `"0"`. Logs: etconsole_r5t1_audit_{set,clean,probe,set2,verify}.log in build\bin\etmain. HARNESS GOTCHA discovered (engine-2010 behavior, NOT a bridge bug): with ET's usesProfiles gameinfo and NO profile, Com_Init execs NO user config at all (common.c:2854-2876 — the `exec etconfig.cfg` else-branch is only for non-profile games) while quit still WRITES etmain\etconfig.cfg; so profile-less harness runs always start at defaults (good for baselines) and archive persistence must be tested with `+set cl_profile <name>`. ND degradation confirmed as predicted: defaults get written as explicit `seta r_x "0"` rows. All experiment state deleted afterward (etmain\etconfig.cfg + profiles\) — baselines captured from clean defaults.*
- [x] **Step 3:** Baseline capture at all-defaults: scripted gl2 runs on oasis and goldrush, 2 fixed setviewpos views each (`r5t1_baseline_<map>_v<1|2>.tga`), plus the same 4 shots under gl1 for reference. Record a baseline timedemo fps number (`+timedemo 1 +demo t4demo` — demos/t4demo.dm_84 exists; if the clean rebuild removed it, copy from C:\Users\chris\Desktop\etrm-r24-evidence\demos\). — *8 shots on disk (build\bin\etmain\screenshots\, 1920x1200 TGA), all verified real in-game frames (pixel means 27.8-149.6, healthy variance; previews eyeballed). CANONICAL VIEWPOINTS — every later task MUST reuse these exact setviewpos values (established R2-4 stations): oasis v1 `setviewpos 144 1900 449 90` (courtyard), oasis v2 `setviewpos -720 1750 501 90`; goldrush v1 `setviewpos 1968 -176 -320 135` (bank front street), goldrush v2 `setviewpos -2184 -972 30 45` (allied-side courtyard). Harness: `+devmap <map> +exec r5t1_base_<map>_<gl>.cfg`, team r 0 5 spawn + god, axis soldier. gl2-vs-gl1 same-view MAD: oasis 12.40/9.73, goldrush 1.89/2.03 (lighting-only differences, geometry matches). Baseline timedemo (gl2, Debug, all defaults): `220 frames, 0.6 seconds: 355.4 fps` (same 220-frame t4demo as R2-4's accepted 401.5 — session variance; relative comparisons within R2-5 should re-run the baseline same-session if precision matters). Logs archived etconsole_r5t1_base_*.log + etconsole_r5t1_timedemo_gl2.log; zero *.dmp across all 10 runs.*
- [x] **Step 4:** Build + ctest 3/3 (always, even doc-only). Commit (`r2-5: baseline lock + cvar flag audit`), plan checkbox ticks with the flag-table findings. — *ctest 3/3 (pmove_feel, fov_math, r2_bridge_bind) green; zero source changes this task — audit passed as-built.*

## Task 2: HDR + tonemap + bloom

**Files:** none expected (runtime bring-up; bridge edits only if broken)

- [ ] **Step 1:** HDR on: launch with `+set r_hdrRendering 1 +devmap oasis`. First gate: log must show the HDR framebuffer path initializing with no GL errors and the HDR_ENABLED caps test passing (FBO+textureFloat+framebufferBlit — all present per R2-3's glConfig2 detection; verify in the log's caps lines). Exercise: outdoor bright sand vs dark interior on oasis — tone mapping should visibly adapt. Screenshots ON vs Task-1 baseline at the same setviewpos (`r5t2_hdr_{off,on}_v1.tga`); pixel-diff must show a real change. Watch specifically for: black screen (FBO misconfig), washed-out output (tonemap-vs-r_gamma double application — R2-4 established gl2 gamma is shader-side via RB_ColorCorrection; their tr_init.c has a known "FIXME: r1 & r2 default values differ" near r_gamma), and console GL error spam.
- [ ] **Step 2:** Bloom on (independent of HDR first: `r_bloom 1` live-toggled, default r_bloomBlur 1.0): bright light sources / sky should glow. Then HDR+bloom together. Screenshots both combos. If bloom only works under HDR (or vice versa), document the dependency rather than forcing it.
- [ ] **Step 3:** Demo determinism + perf: timedemo with HDR+bloom ON vs Task-1 baseline number; record the cost. No bar to pass — just honest numbers for the user.
- [ ] **Step 4:** Off-state regression: re-shoot the Task-1 views at all-defaults; pixel-compare to the Task-1 baselines (expect match within noise — define noise as the same-state re-shoot variance you measure here). ctest 3/3. Commit with evidence summary. If broken beyond a bridge-level fix: document as known-off with the failure evidence (descope mechanism) and move on.

## Task 3: Shadow mapping + per-pixel dynamic lights

The riskiest task (FBO formats, AMD driver variance, cascade math). Baseline note: `r_shadows 1` (blur mode) and `r_dynamicLight 1` are ALREADY the accepted default — this task brings up the MAPPED shadow modes and verifies the dynamic-light quality knobs.

**Files:** none expected

- [ ] **Step 1:** Inventory their r_shadows mode enum (vendored tr_local.h / shadowingMode_t — what do values 2..5 mean exactly; which is "shadow mapping" vs "stencil") before flipping anything. Record the map.
- [ ] **Step 2:** Sun/world shadows: `+set r_shadows <mapping mode> +devmap oasis` (outdoor sun map). Look for: world geometry casting/receiving directional shadows, cascade seams (r_parallelShadowSplits=2 default), acne/peter-panning (r_shadowOffsetFactor/Units are CHEAT tuning — note values that help but do NOT change defaults). Screenshots ON/OFF same viewpoint, plus a player-model shadow shot (third person).
- [ ] **Step 3:** Soft shadows: r_softShadows 1 (ESM) then a PCF value (2-6), with r_shadowMapQuality at default 3. Compare edge quality in screenshot crops. Known upstream gap: PCSS blocker search is unfinished (their forwardLighting_fp.glsl:444 TODO) — soft ≠ contact-hardening; document what the user actually gets.
- [ ] **Step 4:** Dynamic light shadows under the mapped modes: dark-area grenade/muzzle-flash exercise on goldrush (the R2-4 t3g scenario script can be reused from the evidence bundle cfgs); confirm moving lights cast moving shadows without GL errors or fps collapse. r_dynamicLightShadows 0/1 A/B.
- [ ] **Step 5:** Stability: 5+ min exercised session with shadow mapping + soft shadows ON (zero dmps, no per-frame warnings); timedemo cost number. Off-state regression vs Task-1 baseline; ctest 3/3; commit (or document-as-known-off per mode that fails, with evidence).

## Task 4: Material extensions (normal/specular/parallax) + water/reflection disposition

**Files:**
- Create: `etmain/rm_test/textures/rm_test/*` + `etmain/rm_test/scripts/rm_test.shader` — a MINIMAL test material (one diffuse + one hand-made _nm normal map + _s specular, applied via a shader script usable on a devmap surface or a misc_model... simplest workable: override ONE stock texture via the zz_rm pk3 mechanism with a material that adds normal/specular stages, verification-only, clearly named) — wire into `cmake/RmAssets.cmake` autopack ONLY if a pk3 route is chosen; otherwise loose files under fs_homepath etmain (sv_pure 0 harness loads them) and NOTHING ships. Prefer the loose-file route: zero shipping-asset risk.
- Possibly modify: nothing else

- [ ] **Step 1:** No-op proof on retail: `+set r_normalMapping 1` (+vid_restart semantics — command line), oasis baseline views. Screenshots must match the Task-1 baseline within the Task-2-measured noise (the research says stock content lacks _nm/_s and there is no fallback generation — verify that claim empirically). Same for `r_parallaxMapping 1` (requires normalMapping per their tr_glsl.c gate) and the specular cvars. Any unexpected visual change or warning spam = investigate before proceeding.
- [ ] **Step 2:** Positive proof: drop a loose test material into `C:\repo\et-rm\build\bin\etmain\` (homepath, never the retail dir): a `scripts/rm_test.shader` that retargets one well-known oasis wall texture to a material with explicit normal/specular stages (copy the diffuse TGA out of pak0 via python zipfile INTO THE HOMEPATH ONLY, author a simple tangent-space normal map TGA — e.g. a bump-grid generated with numpy — and a specular map). With r_normalMapping 1: the wall must show per-pixel light response (shoot a flare/stand with muzzle flash near it or rely on a nearby dlight); screenshot ON vs OFF. This proves the code path works even though retail content can't show it. Document that real content arrives with R2-6 mapper hooks. Clean up: the loose files stay in homepath (gitignored build dir) — commit only the shader/normal-map GENERATOR script if one was written (docs/superpowers/evidence-scripts/ is fine) so the proof is reproducible.
- [ ] **Step 3:** Water/reflection: locate r_reflectionMapping's actual consumers in the vendored tree (research flagged "registered but consumer not located"). If it drives nothing reachable with retail/ET content (likely upstream-dead or material-dependent), document the disposition (known-inert, file:line evidence) and DO NOT chase it further — YAGNI. If it does work on something (siwa oasis water?), screenshot it.
- [ ] **Step 4:** Off-state regression + ctest 3/3 + commit with the no-op-proof and positive-proof evidence.

## Task 5: Bonus post-effects sweep (keep what works)

User decision 2026-06-12: verify the extras, keep the working ones, document the broken ones as known-off.

**Files:** none expected

- [ ] **Step 1:** Each live-toggleable effect, one at a time, on the oasis baseline view, screenshot ON/OFF + pixel-diff + log grep: `r_screenSpaceAmbientOcclusion 1` (then 2), `r_depthOfField 1`, `r_cameraPostFX 1` (master for vignette/film grain — also try r_cameraFilmGrainScale variations), `r_rotoscope 1` (novelty cartoon mode — verify it doesn't crash, that's the bar). For each: WORKS (visible, stable, no errors) / COSMETIC ISSUE (works with artifacts — describe) / BROKEN (no effect or errors — file:line of the consumer checked, evidence).
- [ ] **Step 2:** Combination sanity: HDR + bloom + shadow mapping + SSAO together (the "everything on" showcase config) — 5 min exercised play, screenshots, fps number vs baseline. This is the config the user will likely show off; make sure it holds together.
- [ ] **Step 3:** ctest 3/3; commit the verdict table into the plan ticks.

## Task 6: Off-state parity gate, docs, finalize, hand off

- [ ] **Step 1:** Final off-state gate: fresh launch at ALL defaults (delete the harness etconfig.cfg first so archived experiments can't leak in — fs_homepath etmain\profiles too if present), re-shoot all 4 Task-1 baseline views, pixel-compare. Must match within measured noise. Also confirm `cvarlist` shows every modern cvar at its default and etconfig.cfg contains none of them set non-default.
- [ ] **Step 2:** Write `docs/MODERN-EFFECTS.md`: the user-facing switch reference — per feature: cvar(s), values, live-vs-vid_restart, what it looks like on stock content, measured fps cost, known limitations (PCSS gap, materials need content, anything descoped as known-off with why). Same sober tone as the README. Link it from README's renderer bullet (one line: "The modern renderer's optional effects are documented in docs/MODERN-EFFECTS.md").
- [ ] **Step 3:** Clean from-scratch build (preserve evidence to C:\Users\chris\Desktop\etrm-r25-evidence\ first: screenshots, archived logs, the test-material generator) + ctest 3/3 + post-build gl2 smoke. gl1 final regression (boot→oasis walk, clean log). Engine-touch audit: `git diff main...HEAD --stat` — expected: plan doc, docs/MODERN-EFFECTS.md, README one-liner, possibly tr2_bridge_theirs.c (flag fix), evidence-script files; vendored diff EMPTY (or THIRDPARTY rows if a mechanical edit proved unavoidable).
- [ ] **Step 4:** Tick plan checkboxes; commit. Hand off for USER acceptance (suggested script: oasis with the everything-on showcase config, then toggle each feature live where possible; verify default boot still looks exactly like R2-4). Do NOT merge/tag — after acceptance: merge `--no-ff` to `main`, tag `rm-r2-5`.

---

## Self-Review
Spec coverage: HDR/tonemap ✓(T2), bloom ✓(T2), shadow mapping + soft + cascades ✓(T3), per-pixel dynamic lights ✓(T3), normal/specular/parallax ✓(T4 with both no-op proof and positive proof), water/reflection ✓(T4 disposition step), SSAO/DoF/cameraFX extras ✓(T5, per user decision), per-feature cvars all default-off ✓(research-verified + T1 flag audit + T6 final gate), off-state reproduces R2-4 ✓(T1 baseline + per-task regression + T6 gate), descope mechanism ✓(known-off documentation path in every task), user A/B acceptance ✓(T6). The one structural risk (cvar flag narrowing breaking LATCH/ARCHIVE semantics) is cleared FIRST in T1 before any feature work depends on it. No placeholders; cvar names/values match the research tables throughout; evidence and harness conventions carried verbatim from the proven R2-4 plan.
