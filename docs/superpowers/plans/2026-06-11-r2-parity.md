# R2-4: ET Feature Parity under gl2 (Playable Milestone) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A full round of ET is playable under `cl_renderer gl2` with every retail-content feature rendering correctly — MDS/MDM skeletal players, MD3/MDC models, decals/marks, dlights, coronas, flares, foliage, atmospheric polys, polybuffers, gradient pics, global fog, gamma — plus working demo playback; verified by A/B screenshots vs gl1.

**Architecture:** R2-2 vendored ALL of ET:Legacy v2.84.0's renderer2 and R2-3 brought up the world, so every R2-4 feature is **already compiled into the DLL with live bridge forwarders** (inventory verified 2026-06-11: tr_model_mds/mdm/md3/mdc, tr_decals, tr_flares, tr_fog, foliage in tr_bsp/tr_surface, RE_AddPolyBufferToScene, RE_StretchPicGradient — all in the build; ProjectDecal/ClearDecals/AddCorona/AddPolyBuffer/SetGlobalFog/LerpTag all forwarded). This increment is therefore **verification and bring-up, not porting**: one known code gap to fix first (the bridge's `CM_PointContents` refimport is stubbed to return 0 — used by their tr_bsp.c:8156/8270 light-grid solid checks, so light sampling is silently wrong on every map), then systematic runtime exercise of each feature with the established debug loop.

**Tech Stack:** as R2-2/R2-3 (MSVC x64 via vcvars64, CMake/Ninja, GLEW+zlib FetchContent, the bridge). Reference clone: `C:\repo\et-deps\etlegacy` (v2.84.0 @ 764ffc00).

**Edit rules (unchanged from R2-2/R2-3):** vendored `etl*` code untouchable (mechanical MSVC/x64-only edits allowed, logged in docs/THIRDPARTY.md); our engine off-limits except provably gl2-gated paths OR additive-only changes verified harmless to gl1 (Task 1 is one — see its gl1-safety argument); gl1 stays byte-identical; `ctest` (pmove_feel golden c1a6e7471a5e279d + fov_math + r2_bridge_bind) green after every task.

**Build (PowerShell tool, NOT Bash):**
```
cmd /c '"C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat" >nul 2>&1 && cmake -S C:\repo\et-rm -B C:\repo\et-rm\build -G Ninja -DCMAKE_BUILD_TYPE=Debug && cmake --build C:\repo\et-rm\build && ctest --test-dir C:\repo\et-rm\build --output-on-failure'
```
**Launch recipe (gl2):** `build\bin\etrm.x64.exe +set fs_basepath "C:\repo\enemy-territory-RM" +set fs_homepath "C:\repo\et-rm\build\bin" +set sv_pure 0 +set r_fullscreen 0 +set developer 1 +set logfile 2 +set cl_renderer gl2` (+ scenario-specific `+devmap` etc.). Log: `build\bin\etmain\etconsole.log`. gl1 regression = same minus `cl_renderer` (or `+set cl_renderer gl1`). NEVER write to `C:\repo\enemy-territory-RM`.

**Git:** branch `rm/09-r2-parity` off `rm/main`. Git via Bash tool. Commit trailer (exact last line):
```
Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

---

## Task 1: Wire CM_PointContents (and un-stale CM_DrawDebugSurface) through the bridge

The only known *code* gap. Their tr_bsp.c light-grid setup calls `ri.CM_PointContents(origin, 0)` to skip light-grid points inside solid; our bridge stub returns 0 (never solid) → subtly wrong ambient/directed light on every map. Also: the bridge no-ops `CM_DrawDebugSurface` with a comment claiming it's "not on our v9 ri" — it IS (`src/renderer/tr_public.h:199`, seeded at `cl_main.c:3235`); wire it (used by their r_showSurfaces-class debug paths) or at minimum fix the false comment.

**gl1-safety argument (engine touch):** appending one function pointer to the END of `refimport_t` and seeding it in CL_InitRef is additive-only — gl1's statically-linked renderer never reads the new field, its existing field offsets are unchanged (append-only), and no gl1 code path is edited. REF_API_VERSION bumps 9→10 so a stale DLL can't bind a mismatched struct; gl1 and the bridge compile from the same tree so both sides move together.

**Files:**
- Modify: `src/renderer/tr_public.h` (refimport_t append + REF_API_VERSION 9→10)
- Modify: `src/client/cl_main.c` (seed `ri.CM_PointContents` in CL_InitRef, next to the existing `ri.CM_DrawDebugSurface` at ~3235)
- Modify: `src/renderer2/bridge/tr2_bridge.h` (neutral-contract entry), `tr2_bridge_ours.c` (BrdgOur callback), `tr2_bridge_theirs.c` (replace the stub + the DrawDebugSurface no-op)

- [ ] **Step 1:** In `src/renderer/tr_public.h`: change `#define REF_API_VERSION 8`-era value 9→10; append to the END of `refimport_t` (after the R2-1 GLimp/GL_GetProcAddress block):
```c
	// RM R2-4: renderer2's light grid needs real solid checks (bridge stub returned 0)
	int ( *CM_PointContents )( const vec3_t p, clipHandle_t model );
```
- [ ] **Step 2:** In `src/client/cl_main.c` CL_InitRef, next to `ri.CM_DrawDebugSurface = CM_DrawDebugSurface;`: add `ri.CM_PointContents = CM_PointContents;` (prototype already visible via cm_public.h — same header that declares CM_DrawDebugSurface). Mirror in `CL_SetPlatformRefImport` ONLY if that function seeds CM_* (check; it likely seeds only platform entries — then no change there).
- [ ] **Step 3:** Bridge plumbing following the existing per-entry pattern exactly: `tr2_bridge.h` neutral typedef (primitives only: `int (*CmPointContents)(const float *p, int model);`); `tr2_bridge_ours.c` `BrdgOur_CmPointContents` calling our `rip->CM_PointContents`; `tr2_bridge_theirs.c` `imp_CM_PointContents` now forwarding (delete the logged-once stub). For `imp_CM_DrawDebugSurface`: forward the same way (the drawPoly callback signature is identical primitive types on both sides, passable through the neutral contract verbatim) — or if the fn-pointer-through-fn-pointer plumbing turns out disproportionate, keep the no-op but CORRECT the comment and log why. Update the GetRefAPI version the bridge exports/accepts (9→10) and the bind harness if it asserts the version.
- [ ] **Step 4:** Build + ctest 3/3. gl2 launch `+devmap oasis`: grep log — the "CM_PointContents stubbed" warning GONE; map loads/walkable. gl1 regression launch `+devmap oasis` silent-clean. Commit (`r2-4: wire CM_PointContents through the bridge (real light-grid solid checks)`).

## Task 2: Skeletal players + model formats in-world (MDS/MDM, MD3/MDC) with gl1 A/B

The bone-math risk center. Their LerpTag dispatches to RE_LerpTagET → R_GetBoneTag (MDS) / R_MDM_GetBoneTag (MDM); `USE_REFENTITY_ANIMATIONSYSTEM=1` is set in our CMake — confirm that matches what upstream ETL builds with for ET content (check their CMakeLists in the reference clone; if upstream ships renderer2 WITHOUT it for ET retail content, flip ours to match — this define swaps animation code paths and is the most likely source of wrong-bones).

- [ ] **Step 1:** Verify the `USE_REFENTITY_ANIMATIONSYSTEM` setting against the reference clone (`C:\repo\et-deps\etlegacy\src\renderer2\CMakeLists.txt` + grep their code for the define). Align ours; document the decision in the commit message.
- [ ] **Step 2:** Exercise loop on `+devmap oasis`: spawn each class (console: `class m 1` etc. or limbo UI), `cg_thirdperson 1` to view own MDS/MDM player model + animations (idle/run/crouch/prone, weapon switch); limbo menu head model (MDM head); pickup/weapon models (MD3/MDC — drop weapon, ammo/med packs via `give all`). Watch the log for model-load warnings ("RE_LerpTag", "no such bone", shader warnings). Fix loop per edit rules.
- [ ] **Step 3:** A/B protocol: identical scripted runs under gl1 and gl2 — `+devmap oasis +set cg_thirdperson 1 +set cg_thirdpersonrange 120 +wait 600 +screenshot +wait 50 +quit` (and a second view: `+set cg_thirdpersonangle 180`). Compare TGA pairs (build\bin\etmain\screenshots) — player silhouette/pose must match gl1 (lighting may differ; GEOMETRY must not). Save the pairs for the user's acceptance review. Iterate on mismatches (bone math, axis conventions, torso/legs blending).
- [ ] **Step 4:** ctest 3/3 + gl1 regression. Commit per milestone.

## Task 3: Effects parity sweep — decals, dlights, coronas, flares, fog, foliage, atmospherics, polybuffers, gradients

Everything is forwarded and compiled; this is the systematic exercise + fix loop. Map assignments target where retail content actually uses each feature:

| Feature | Exercise | Map |
|---|---|---|
| Bullet marks / explosion decals (ProjectDecal/MarkFragments) | shoot walls/floor, grenade, panzer | oasis |
| Dynamic lights | muzzle flash, explosions, flak | oasis |
| Coronas | light fixtures (night map = visible) | goldrush |
| Flares | sun/lights where present (`r_flares 1`) | oasis |
| Global/volume fog (SetGlobalFog/SetFog) | fog volumes + fog hints | fueldump, radar |
| Foliage (SF_FOLIAGE) | grass fields (`r_drawfoliage 1` default) | radar |
| Atmospherics (polybuffers!) | `cg_atmosphericEffects 1`: snow on fueldump, rain on radar — cgame feeds these through RE_AddPolyBufferToScene, so this exercises polybuffers too | fueldump, radar |
| Gradient pics | stock HUD/UI elements (DrawStretchPicGradient) | any |

- [ ] **Step 1:** Per row: gl2 launch on the assigned map, exercise (scripted +wait/+screenshot where possible, manual binds where interaction is needed — `give all`, fire weapons), capture screenshot evidence + grep log for renderer errors/warnings spam. A feature that renders wrong/missing/black → debug loop (bridge/scoped config first; vendored mechanical+logged; engine off-limits except provably gl2-gated). Their `tr_decals.c`/`tr_flares.c`/`tr_fog.c` vs gl1's equivalents in the reference clone are the diff-debugging source.
- [ ] **Step 2:** A/B screenshot pairs (gl1 vs gl2, same scripted view) for: a decaled wall, foliage field on radar, fog on fueldump, atmospherics ON. Geometry/placement must match; gl2 lighting differences are expected and acceptable.
- [ ] **Step 3:** Console hygiene bar: a 5-minute exercised session produces NO repeating per-frame warnings in the log. ctest 3/3 + gl1 regression after any shared-file touch. Commit per feature-group milestone (`r2-4: decals+dlights verified under gl2`, ...).

## Task 4: Gamma + demo playback

- [ ] **Step 1:** Gamma: determine which path is live under gl2 — hardware ramp via the bridged `GLimp_SetGamma` vs their shader-side gamma (tr_backend.c UNIFORM_GAMMA, r_gamma cvar). Exercise `r_gamma 1.3` / `r_gamma 1.0` at the gl2 menu + in-world: visible brightness change, no crash, value persists across vid_restart. If both paths fight (double-gamma), prefer their shader path under gl2 and make the bridge's SetGamma a documented no-op for gl2 ONLY (gl1's ramp path untouched).
- [ ] **Step 2:** Demo: record a short demo under gl2 (`+devmap oasis`, in-game: `/record r2demo`, move/shoot ~30s, `/stoprecord`); play it back under BOTH renderers: `+demo r2demo` with `cl_renderer gl2` then `gl1`. Bar: playback runs to completion, world+players render, no ERR_DROP. (Demos are renderer-agnostic network streams — this is a pure renderer exercise and doubles as a deterministic A/B source.)
- [ ] **Step 3:** ctest 3/3 + gl1 regression. Commit.

## Task 5: Full-round smoke, finalize, hand off

- [ ] **Step 1:** Long-run smoke under gl2: goldrush, `+devmap goldrush`, 10+ minutes exercised play (objectives area, tank area, weapon variety, limbo in/out, class switches) — zero crashes, zero minidumps (check build\bin for *.dmp), log clean of repeating errors. Note approximate fps vs gl1 on the same scenes (bar from the roadmap: within 2x).
- [ ] **Step 2:** Clean from-scratch build (delete build dir, full reconfigure) + ctest 3/3. THIRDPARTY.md rows for any new vendored edits (target: zero new ones). Update docs if the gamma decision (Step 4.1) or any stub disposition changed.
- [ ] **Step 3:** gl1 final regression: full boot→menu→oasis walk, byte-faithful behavior. Feel harness green (it ran every task; confirm once more).
- [ ] **Step 4:** Hand off for USER acceptance: play a full round under gl2 (suggest goldrush), review the A/B screenshot pairs (especially player models — bone math), try `cg_atmosphericEffects` on fueldump/radar, demo playback. Then merge `--no-ff` to `rm/main`, tag `rm-r2-4`, push.

---

## Self-Review
Roadmap R2-4 scope check: MDS/MDM ✓(T2), MD3/MDC ✓(T2), foliage ✓(T3), decals/MarkFragments/ProjectDecal ✓(T3), coronas ✓(T3), dlights ✓(T3), flares ✓(T3), polybuffers ✓(T3 via atmospherics + table row), atmospheric polys ✓(T3), gradient pics ✓(T3), gamma ✓(T4), full round playable ✓(T5), demo playback ✓(T4), A/B screenshots vs gl1 for bone math ✓(T2/T3), feel green ✓(every task). Known-gap CM_PointContents fixed first (T1) with an explicit gl1-safety argument for the engine touch. No placeholders; debug loops bounded by the same proven R2-2/R2-3 edit rules + escalation. The one genuinely risky unknown (USE_REFENTITY_ANIMATIONSYSTEM vs upstream's ET configuration) is checked BEFORE the bone-math exercise rather than discovered through it.
